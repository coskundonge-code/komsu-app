# TECH_DEBT.md — Teknik Borç & Kabul Edilen Riskler

> Kural (AI Architecture Rules / K2+K12): Borç sıfır olmak zorunda değil; **görünür ve
> planlı** olmalı. Her madde: ne · nerede · etki · neden ertelendi · ödeme planı + tarih.
> Son güncelleme: 2026-06-07

## Kabul edilen riskler (Faz 0'da bilinçli bırakıldı)

### 1. Test aracında 2 "critical" npm açığı (dev-only)
- **Ne:** `npm audit` → `vitest` + `@vitest/ui` (v2) zincirinde 2 critical + birkaç moderate.
- **Nerede:** Yalnızca `devDependencies` (test koşucusu). Üretim paketine (production bundle) **girmez**, kullanıcıya ulaşmaz.
- **Etki:** Düşük (geliştirici makinesi/CI ile sınırlı).
- **Neden ertelendi:** Açığı kapatan tek sürüm `vitest@4`, beraberinde `rolldown` native-binding bağımlılığı getiriyor; bu Windows-dev + Linux-CI + Vercel'de çapraz-platform kırılganlık yarattı (npm bug #4828, native binding bulunamadı). Sadelik ilkesi gereği stabil `vitest@2`'de kalındı.
- **Ödeme planı:** `vitest@4` + `rolldown` ekosistemi olgunlaşınca yükselt; veya native-binding gerektirmeyen koşucuya geç. **Hedef: Faz 2.**

### 2. Üretimde 3 "moderate" npm açığı (postcss zinciri)
- **Ne:** `postcss` → `next` → `@vercel/analytics` üzerinden 3 moderate.
- **Nerede:** Build/transitive bağımlılık.
- **Etki:** Düşük-orta.
- **Neden ertelendi:** npm'in önerdiği tek "çözüm" Next'i kıracak şekilde geri almak (`--force`); temiz ileri yönlü yama yok.
- **Ödeme planı:** Yeni `next`/`@vercel/analytics` sürümlerini izle, çıkınca yükselt. **Hedef: Faz 1 sonu kontrol.**

## Faz 1 — Yayın öncesi kapatılacak borçlar

### 3. PayTR callback hardening (güvenlik, derinlemesine savunma)
- **Yapıldı (2026-06-06):** `status` `{success,failed}` allowlist'i + `total_amount` sayısal (`NaN`) doğrulaması eklendi → geçersiz değerler artık 400 döner ve DB'ye yazılmaz. Doğrulanmış tutar tek yerde hesaplanıp iki upsert'te paylaşılıyor. Callback zaten tip-denetimliydi (`@ts-nocheck` yok). ✅
- **Nerede:** `src/app/api/payment/callback/route.ts`.
- **Yapıldı (2026-06-07) — merchant_oid ayrıştırma çıkarıldı + test edildi:** Callback'in en hataya-açık saf mantığı olan `merchant_oid → (payment_type, userId)` çözümü route'tan **sıfır-import saf modüle** taşındı: `src/lib/services/paytr-oid.ts` → `parseMerchantOid()` (K1: ince route, test edilebilir domain). Route artık tek satır (`const { paymentType, userId } = parseMerchantOid(merchantOid)`), davranış birebir korundu. 12 yeni test (`src/__tests__/paytr-oid.test.ts`) para-atfını koruyor: 3 gerçek tür (mahalle_card/listing_fee/business_membership), UUID bütünlüğü (`split('_')` tireyi bozmuyor), üretim biçimiyle round-trip, aktivasyona uygunluk, **bozuk/kısa OID → boş userId** (yanlış kullanıcı aktive edilmez), ve örtük allowlist kırılganlığı regresyon guard'ı (yeni iki-kelimeli tür eklenirse `TWO_WORD_SECOND_TOKENS` güncellenmeli). `next build` 0, test 162/162, lint 0. ✅
- **Yapıldı (2026-06-07) — route handler entegrasyon testi:** Callback POST'unun kendisi uçtan uca test edildi (`src/__tests__/payment-callback.test.ts`, 9 test): mocklu `@supabase/supabase-js` + sahte `formData` ile (a) **güvenlik kapıları** — eksik parametre / hash uyuşmazlığı / imzalı-tutar-değişimi / bilinmeyen durum / sayısal-olmayan tutar hepsi **400 döner ve DB'ye dokunmaz** (`fromMock` çağrılmaz); (b) **yan etkiler** — mahalle_card & business_membership success → `payments` completed (kuruş→TL) + DOĞRU kullanıcıya profil aktivasyonu (`update().eq('id', userId)`), listing_fee success → aktivasyon YOK, failed (mahalle_card olsa bile) → aktivasyon YOK. Sahte bir POST ile bedava kart/üyelik veya yanlış-kullanıcı aktivasyonu artık testle kilitli. `next build` 0, test 171/171. ✅ **#3 kapandı.**
- **Yapıldı (2026-06-07) — test-dosyası tip kapısı sıkılaştırıldı (`payment-callback.test.ts` yanlış-yeşili yakalandı):** Taze `npx tsc --noEmit --incremental false`, bir önceki turda eklenen `payment-callback.test.ts`'te 2 gizli tip hatası açığa çıkardı (satır 153/168: `updateMock = vi.fn(() => …)` **sıfır-argüman** imzası çıkarsadığı için `.mock.calls[0][0]` boş-tuple TS2493/TS2352 hatası veriyordu). **Neden gizlenmişti:** test dosyaları `next build` grafiğinde DEĞİL (build onları tip-denetlemez) + `tsconfig` `incremental:true` olduğundan bayat `.tsbuildinfo` ile standalone `tsc` **yanlış-yeşil** verdi. CI'ın ZORUNLU `Typecheck` adımı (`npx tsc --noEmit`) taze checkout'ta (TS cache yok) bunu yakalardı → **bir sonraki `coskun` push'unda CI kırmızı olurdu.** Düzeltme: mock imzasına gerçek argüman verildi (`vi.fn((_data?: Record<string,unknown>) => …)`) — üretimdeki `update({...})` çağrısıyla birebir, davranış değişmedi. **Ders (line ~115 keskinleştirildi): üretim kodu için yetkili gate `next build`; ama TEST dosyaları build grafiği dışında olduğundan onlar için yetkili yerel gate `tsc --noEmit --incremental false` (CI'ın taze `tsc`'siyle eşdeğer).** tsc 0, test 179/179. ✅

### 4. Tip güvenliği borcu (~96 `tsc` hatası)
- **Ne:** `npx tsc --noEmit` → 96 hata (çoğu admin sayfalarında implicit `any` (TS7006), ayrıca `use-listings.ts`/`use-posts.ts`'te gerçek tip uyumsuzlukları).
- **Etki:** Yüksek — bu hatalar muhtemelen `next build`'i de kırıyor (`next.config.ts` → `ignoreBuildErrors:false`).
- **Ödeme planı:** Tipleri düzelt; `any` sayısını düşür (~197). **Hedef: Faz 1 (öncelikli).**

### 5. Lint borcu (Faz 1'de 356 → 265 indirildi)
- **Yapıldı (2026-06-06):** rules-of-hooks 17 (2 gerçek hook-sıralama bug'ı: pazar/kategori + admin) ✅ · no-unescaped-entities 65 (kozmetik) ✅ · **react-query ambient override kaldırıldı** — `src/types/modules.d.ts` içinde `useQuery(options:any):any` tüm react-query tiplerini `any`'ye eziyordu; implicit-any cascade'inin KÖK NEDENİ idi ✅
- **Kalan (265):** 214 `no-explicit-any` (çoğu supabase join-select'lerinde load-bearing `(data as any[])` cast'leri, ~50 dosyaya yayılı) + 17 set-state-in-effect + ~34 misc (immutability, ban-ts-comment, prefer-const…).
- **Not:** K2 kapısı "borç SIFIR" istemez; "görünür + planlı" ister (bu kayıt onu sağlar). Kalan `any`'yi sıfırlamak market-ready için ZORUNLU değil.
- **Ödeme planı:** Bulk `any`'yi köken azaltmanın kaldıracı = types.ts'i canlı şemadan yeniden üret (supabase sonuçları tiplenince `as any[]` gereksizleşir) → kalan cast'leri kaldır. Dosya dosya da eritilebilir. CI lint bu süre informational. **Hedef: kademeli.**

### 6. Tanrı-dosyalar (>500 satır)
- **Ne:** `askida-bagis/page.tsx` (1994 — **artık "yakında" ile kapatıldı**, bkz. #9), `kayit/page.tsx` (1272), `pazar/ilan-ver/page.tsx` (1130) + 9 dosya 800–999.
- **Ödeme planı:** Bileşen + hook'a böl; veri çekmeyi servis/hook katmanına taşı. `kayit` (kayıt hunisi/eDevlet) en riskli — runtime testi gerektirir, sahip başındayken yapılmalı. **Hedef: Faz 1.**

### 7. CI kapıları (2026-06-06 sıkılaştırıldı)
- **Yapıldı:** `typecheck (tsc --noEmit)` + `next build` + `test` artık ZORUNLU kapı (kırmızı = push'ta görünür/CI patlar). ✅
- **@ts-nocheck servisleri:** address-verification + review-system temizlendi (tip denetimi açık) ✅. Hâlâ kapalı: payment, payment/callback, listing-quota, business-subscription, content-moderation — hepsi bayat types.ts yüzünden; **types.ts regen** ile açılacak (bkz. #8).
- **Hâlâ informational:** `lint` (~253 kalan borç) + `npm audit` (3 prod-moderate).
- **Ödeme planı:** lint borcu eriyince lint'i, üretim açığı kalmayınca audit'i de zorunlu yap.

### 8. Şema kayması ÇÖZÜLDÜ + eksik-özellik tabloları OLUŞTURULDU
- **types.ts canlı şemadan yenilendi** → 3 `as any` + 5 servis `@ts-nocheck`'i kaldırıldı (audit_log/user_addresses/businesses tipli).
- **2026-06-06 — 3 eksik tablo migration + RLS ile OLUŞTURULDU:** `user_listing_quotas`, `business_subscriptions`, `payments`. RLS: kullanıcı/işletme-sahibi sadece kendi kaydını görür. types.ts'e tipleri elle eklendi (regen bat'ı büyük dosyada kesiyor — bilinen sorun).
  - **Aktive edildi (artık tip-denetimli):** listing-quota ✅ · business-subscription ✅ · payment/callback (PayTR webhook) ✅ · address-verification ✅ · review-system ✅
- **2026-06-06 — `payment.ts` (intent servisi) AKTİVE EDİLDİ:** Mükerrer `type`/`payment_type` kolonları tek konvansiyona (`payment_type`) indirildi, boş `type` kolonu migration ile düşürüldü; `refunded_at` kolonu eklendi (iade audit izi); `getRevenueReport` enum-dışı tür/durum/yöntem değerlerine (PayTR `mahalle_card` vb.) karşı null-guard'landı; nullable kolonlar mapping sınırında coalesce edildi → `@ts-nocheck` kaldırıldı, `tsc` 0 hata. ✅
- **2026-06-06 — `content-moderation.ts` AKTİVE EDİLDİ:** `content_moderation` tablosu VARDI ama servis canlıda olmayan kolonlara yazıyordu. Eksik kolonlar additive migration ile eklendi (`author_id`+FK, `ai_categories`, `ai_reviewed_at`, `content_snapshot`, `title_snapshot`, `image_urls_snapshot`, `priority`, `auto_approved`); `ai_reasoning`→`reason`, `admin_id`→`resolved_by`, `admin_note`→`admin_notes`, `admin_reviewed_at`→`resolved_at` konvansiyonuna indirildi. **RLS düzeltildi:** insert artık AI akışını (`author_id=auth.uid()`) + şikâyet akışını kabul ediyor, admin'ler tüm kayıtları görüp güncelleyebiliyor (önceki politika AI insert'i runtime'da reddediyordu). types.ts hizalandı → `@ts-nocheck` kaldırıldı, `tsc` 0. ✅
- **Kalan `@ts-nocheck`: 0 dosya.** Tüm servisler tip-denetimli.
- **Not:** Tablo + tip = servisler DERLENEBİLİR/tip-güvenli. Özelliklerin uçtan uca ÇALIŞMASI için UI bağlama + test ayrı bir adım (ürün işi).

### 9. "Askıda Bağış" prototip ödeme akışı geçici olarak kapatıldı (2026-06-07)
- **Ne / nerede:** `src/app/(main)/askida-bagis/page.tsx`. Sayfa ana menü + mobil çekmece + sidebar'dan erişilebilir bir **headline özellik** idi ama ödeme adımı tamamlanmamıştı:
  - **SAHTE kart formu** (kodda açıkça `{/* Credit Card Form (Fake iyzico) */}`, ~satır 1382) gerçek kart numarası (PAN) topluyordu → **PCI/uyumluluk riski** + Apple/Google ret sebebi (K4/K5/K10).
  - Form var olmayan `/api/payment/iyzico` ucuna POST atıyordu → **404** (gerçek ödeme PayTR; donation için PaymentType bile yok). Ödeme sessizce başarısız.
  - DB boşken kullanıcıya **sahte istatistik** gösteriyordu (`items: 47, businesses: 8` sabit).
- **Etki:** Yüksek (gerçek kullanıcı sahte kart formuna kart girip 404 alabilirdi; mağaza reddi riski).
- **Yapıldı:** Sayfa default export'u temiz bir **"Askıda Bağış çok yakında"** ekranıyla değiştirildi (kapı). Tüm çalışma kodu (`AskidaBagisPageContent`) dosyada **korunuyor**; `_AskidaBagisPageGatedContent` içinde referanslı tutuldu. `tsc` 0 hata, `next build` 0. Nav bağlantıları bırakıldı (artık nazik bir "yakında" gösteriyor).
- **Geri açma:** Ödeme PayTR'a bağlanıp (donation için `PaymentType` + backend) uçtan uca test edilince, default export'u tek satırla `return <AskidaBagisPageContent />` yaparak aç.
- **Ödeme planı:** Donation ödemesini PayTR hosted-page akışına bağla (kendi formunda kart TOPLAMA — PCI). **Hedef: Faz 1 (ürün kararı + entegrasyon).**

### 10. Para kazanma (ödeme) katmanı — sahte UI'lar temizlendi; canlı tahsilat hâlâ anahtar+test bekliyor (2026-06-07)
Pazara-hazırlık taraması sırasında tespit edildi. Tek "gerçek" uç `/api/payment` (PayTR) **var ve tip-güvenli**, ama UI'lar tutarsızdı: bir kısmı sahte, bir kısmı test-verisiyle bağlı, bir kısmı dürüstçe "ücretsiz". **Sahte/yanıltıcı yüzeyler bu turda kapatıldı** (aşağıda ✅); geriye kalan iş canlı PayTR anahtarı + uçtan uca tahsilat testi (sahip + anahtar gerektirir).
- **Yapıldı (2026-06-07) — `isletme-paneli/uyelik/page.tsx` GERÇEK servise bağlandı:** Sayfa baştan yazıldı. Artık `useCurrentUser()` ile gerçek kullanıcı, `owner_id` ile gerçek işletme, `checkSubscriptionStatus(business.id)` ile gerçek abonelik durumu (deneme/aktif/süresi-dolmuş) gösteriyor. Ödeme GERÇEK PayTR modaline (`components/payment/payment-modal`) **gerçek kimlikle** (`userId={user.id}`, `userEmail`, `userName`) gidiyor; başarıda `activateSubscription`, iptalde `cancelSubscription` çağrılıyor. Sabit `user_123`/`test@example.com`, sahte aktif-üyelik ve sahte ödeme geçmişi **kaldırıldı**. Fiyatlar gerçek modelle hizalı (aylık `MONTHLY_PRICE`=1900₺ / yıllık `YEARLY_PRICE`=19900₺, 3 ay ücretsiz deneme). ✅
- **Yapıldı (2026-06-07) — `pazar/odeme/page.tsx` nötrlendi + sahte PAN modali emekliye ayrıldı:** Bu orphan sayfa **2 sn bekleyip `success` döndüren SAHTE** akıştı ve `@/components/marketplace/payment-modal` (gerçek kart numarası/CVV TOPLAYAN PCI mayını) kullanıyordu. Default export artık `redirect('/pazar')`; eski prototip referans için `_PaymentPageLegacy`'de korunuyor (erişilemez). Sahte `marketplace/payment-modal` **yalnızca** bu sayfa tarafından import ediliyordu → canlı uygulamadan fiilen kalktı. ✅
- **Yapıldı (2026-06-07) — `esnaf-kayit/page.tsx` gerçek onboarding'e yönlendirildi:** (bkz. #12) Default export `redirect('/isletme-ekle')`; sahte adres-doğrulama (`setAddressVerified(true)`) + çelişen 99₺ modeli içeren eski kod `_EsnafKayitPageLegacy`'de erişilemez. ✅
- **Kalan — `/api/payment` (PayTR):** anahtar yoksa **"simülasyon modu"** döner (`route.ts:66`) — yani anahtar girilene dek hiçbir şey tahsil etmez (güvenli ama "çalışıyor" sanılmamalı).
- **Kalan — `pazar/ilan-ver/page.tsx` (~592):** DÜRÜST — ilan ücretsiz yayınlanıyor, "Ödeme sistemi yakında aktif olacak" toast'ı + `// TODO: payment gateway`. Launch için kabul edilebilir (ücretsiz ilan).
- **Çözüldü — iki ayrı `PaymentModal` karışıklığı:** Sahte `components/marketplace/payment-modal` artık hiçbir canlı yoldan kullanılmıyor (yalnızca erişilemez legacy referansı). Tek gerçek modal `components/payment/payment-modal`. (Temizlik: sahte modal dosyası ileride büsbütün silinebilir — şu an zararsız ölü kod.)
- **Yapıldı (2026-06-07) — ilan kotası iş kuralı test edildi (`listing-quota.test.ts`, 8 test):** Para-bitişiği kota mantığı kilitlendi: `'free'/'lend'` ilanları **DB'ye HİÇ dokunmadan** her zaman ücretsiz (erken-dönüş sözleşmesi — testte `fromMock` çağrılmadığı doğrulanıyor); `'sale'/'rental'` (ve tür-belirtilmeyen varsayılan) yıllık kotaya tabi (mocklu Supabase zinciriyle: free_used=0 → kalan 1 / canPostFree; free_used=1 → ücretli; aşırı kullanım free_used=5 → `remainingFree` 0'a kenetli, asla negatif); fiyat sabitleri (`FREE_LISTING_LIMIT=1`, `LISTING_FEE=9.90`, `CURRENCY=TRY`) sessiz-değişim guard'ıyla sabitlendi. Bir regresyon doğrudan **gelir hatası** olurdu (sale hep-ücretsiz olursa para kaybı; free kota tüketirse kullanıcı ücretsiz ilanından olur). tsc 0, test 179/179. ✅
- **Ödeme planı (kalan):** (1) Ürün kararı: ilan/öne-çıkarma para alacak mı, yalnızca işletme aboneliği mi? (2) PayTR canlı anahtarlarını ENV'e gir. (3) Küçük bir tutarla `isletme-paneli/uyelik` → PayTR → callback → `activateSubscription` zincirini uçtan uca canlı test et. (4) İstenirse sahte `marketplace/payment-modal` dosyasını sil. **Hedef: Faz 1 (yayın öncesi, sahip + anahtar gerektirir).**

### 11. AI görsel moderasyonu fiilen kapalı (fail-open) (2026-06-07)
- **Ne / nerede:** `src/lib/services/content-moderation.ts:663-699` görseller için `/api/moderate-media` ucuna POST atıyor, **ama bu uç YOK** (route dosyası yok). `response.ok` false → blok atlanır → fonksiyon `isApproved: true` döndürür (**fail-open**: AI denetimi geçemediğinde içerik GEÇER).
- **Etki:** Orta-yüksek (kullanıcı yüklemeli görsellerde NSFW/yasadışı içerik AI ön-filtresinden geçmez). Hafifletici: (a) dosya tipi + boyut kontrolleri çalışıyor; (b) insan şikâyet + moderasyon akışı var; (c) çökme yok (graceful, `console.warn`).
- **Karar — fail-open mı fail-closed mı?** Şu an fail-open. Fail-closed yapmak (uç yokken reddet) **tüm görsel yüklemeyi bloke ederdi** — daha kötü. Bu yüzden uç inşa edilene dek fail-open doğru; ama "AI moderasyon aktif" sanılmamalı.
- **Ödeme planı:** `/api/moderate-media` route'unu bir AI vision sağlayıcısıyla (ör. OpenAI moderation / Google Vision SafeSearch) inşa et; sağlayıcı seçimi + anahtar sahip kararı. O zamana dek insan moderasyonu birincil hat. **Hedef: Faz 1 (UGC görseli launch kapsamındaysa öncelikli).**

### 12. Sahte/fallback "mock" veri kümeleri (kullanıcıya yanlış içerik gösterebilir) (2026-06-07)
- **Ne:** ~12 sayfa DB sorgusu boş/başarısız olduğunda **mock (sahte) veriye düşüyor** → gerçek kullanıcı sahte içerik görebilir. Örnekler: `isletmeler/[slug]` ("Moda Fırın" sahte işletme), `pazar/ilan/[id]` + `pazar/kategori/[slug]` (sahte ilanlar), `blog/[slug]` (sahte blog), `favoriler`, `uyarilar`, `profil/[id]`, `ara`, `gruplar/olustur` (sahte üye önerileri), `notification-dropdown` (sahte bildirimler).
- **Etki:** Orta (para/güvenlik değil ama "boş ürün" yerine "sahte dolu ürün" gösterir — pazarda güven kaybı / kafa karışıklığı).
- **Not (güncellendi 2026-06-07):** Admin paneli eskiden mock veriyi `MockDataBanner` ("bu veri örnektir") ile dürüstçe işaretliyordu; artık BUNA GEREK KALMADI — son sahte admin yüzeyi (`admin/odemeler`) gerçek `payments` tablosuna bağlandı, `MockDataBanner` bileşeni + `admin/layout.tsx`'teki bağlama **silindi**. Asıl risk artık yalnızca son-kullanıcı sayfalarındaki sessiz fallback'lerdi; onlar da bu turda büyük ölçüde temizlendi (aşağıya bkz.).
- **Çözüldü (2026-06-07) — İşletme paneli (`isletme-paneli/page.tsx`) sahte widget'ları:** Panel kısmen gerçekti (işletme/abonelik/yorumlar canlı) ama UYDURMA parçalar vardı: ölü "Abone Ol" butonu (onClick yok), sahte istatistik trendleri (`+12.5%`, `₺12,450 gelir`), sahte haftalık aktivite grafiği, sahte "Yaklaşan Etkinlikler", sahte performans yüzdeleri (85/92/96) ve kullanılmayan ölü mock sabitleri (`STATS`, `RECENT_REVIEWS`, `UPCOMING_EVENTS`). **"Abone Ol" gerçek `/isletme-paneli/uyelik` sayfasına bağlandı; uydurma trend/grafik/etkinlik/performans dürüst "yakında" durumuyla değiştirildi; ölü sabitler ve ölü butonlar (Ürün Ekle / Cevap Ver / Raporla) silindi.** Artık panel yalnızca gerçek veriyi gerçek, henüz olmayan veriyi "yakında" gösteriyor. `tsc` 0, build 0. ✅
- **Çözüldü (2026-06-07) — Esnaf-kayıt sahte doğrulaması:** `esnaf-kayit/page.tsx:335` adres doğrulama butonu **sahteydi** (`onClick={() => setAddressVerified(true)}`, gerçek e-Devlet çağrısı yok → tek tıkla "doğrulanmış" rozeti). Bu sayfa zaten `/isletme-ekle` (gerçek onboarding: gerçek `insert` + `startFreeTrial`) tarafından ASILMIŞTI ve oraya hiçbir yerden link yoktu. Default export `redirect('/isletme-ekle')` yapıldı; sahte kod erişilemez `_EsnafKayitPageLegacy`'de korunuyor. Gerçek kullanıcı artık sahte rozet üreten bu akışa ulaşamıyor. ✅
- **Çözüldü (2026-06-07) — `pazar/page.tsx` (amiral gemisi pazar) sahte fallback'i:** Sayfa gerçek veriyi `useListings({status:'active',limit:50})` ile çekiyordu ama DB boşsa `listings.length > 0 ? listings : mockListings` ile **sahte ilanlara düşüyordu** (iki yerde: ana filtre + öne çıkanlar). Ayrıca her ilan kartına UYDURMA alanlar enjekte ediliyordu: `featured: Math.random() > 0.7` ve sahte yıldız `★ {rating} ({reviewCount})`. **İki mock fallback kaldırıldı → artık doğrudan `listings`; DB boşsa dürüst boş-durum gösteriliyor ("Henüz ilan yok · ilk ilanı sen ver" + İlan Ver linki) + yükleniyor durumu; `featured` artık gerçek `item.is_featured ?? false`; uydurma yıldız bloğu silindi.** `tsc` 0, test 141/141, build 0 (`/pazar` ○ Static). **Temizlik tamamlandı (2026-06-07):** `mockListings` ölü sabiti + demo görsel (`getFeedImageUrl`) fallback'i + uydurma `distance`/rating ve var olmayan `is_featured` karuseli **tamamen kaldırıldı** (dosyada artık ölü kod yok; `demo-images` importu yok). Mesafe filtresi de kaldırıldı (kullanıcı konumu/haversine olmadan gerçek mesafe hesaplanamıyordu). ✅
- **Çözüldü (2026-06-07) — `odunc-kirala/page.tsx` sahte fallback'i:** Pazar ile aynı kalıp: `getLendingItems` çekiyordu ama hata/boş durumda `setListings(mockListings)` + filtrede `listings.length > 0 ? listings : mockListings` ile **sahte ilanlara** düşüyordu; her kartta uydurma yıldız (`rating: Math.random()*2+3` — yuvarlanmamış çirkin ondalık olarak gösteriliyordu) vardı. **İki mock fallback `[]`/gerçek `listings` ile değiştirildi (boş-durum zaten mevcut: "İlanlar bulunamadı"); uydurma rating bloğu + kullanılmayan `Star` importu silindi.** Temizlik tamamlandı (2026-06-07): `mockListings` ölü sabiti + uydurma `distance`/rating + `demo-images` importu **tamamen kaldırıldı** (dosyada ölü kod yok). ✅
- **Çözüldü (2026-06-07) — `mahallem-kart/page.tsx` tamamen sahte sadakat özelliği → dürüst "çok yakında":** Sayfa her kullanıcıya **sahte 1250 puan / 23 indirim / 8 bağış** gösteriyordu ve gerçek işletmelere **uydurma indirim oranları** (`5 + index%15`) atfediyordu (Moda Fırını %10 — işletme böyle bir taahhüt vermedi → gerçek sorumluluk/güven riski). Canlı şemada `mahallem_cards`, `business_discounts`, `card_transactions` tabloları VAR ama **tamamen boş** (0 kart/indirim/işlem) ve kart-verme + puan-biriktirme + itfa (redemption) mantığı **hiç yazılmamış**. En dürüst ve en yalın temsil: default export **dürüst "çok yakında" sayfasıyla** değiştirildi (esnaf-kayit kalıbıyla aynı); tam UI erişilemez `_MahallemKartPageLegacy`'de korunuyor. **Canlıya almak için kalan iş (ürün kararı gerektirir, sahip çevrimdışıyken yapılmadı):** kart issuance + puan accrual kuralları + indirim itfa/QR doğrulama + esnaf tarafı indirim yönetimi UI. ✅
- **Çözüldü (2026-06-07) — `pazar/kategori/[slug]/page.tsx` tamamen sahte + bozuk SEO sayfası gerçeğe bağlandı:** Sayfa DB'ye HİÇ sormuyordu; `mockListings` (10 sabit elektronik) → `filter(l => l.category === slug)` ile sadece `elektronik` slug'ı dolu, diğer tüm kategoriler boş görünüyordu. Ayrıca `categoryMap` slug'ları (`spor`, `kitap`) canlı slug'larla (`spor-hobi`, `kitap-kirtasiye`) uyuşmuyordu ve `sitemap.ts` hiç var olmayan slug'lara (`elbise-giyim`, `cocuk-urunleri`, `bilgisayar-teknoloji`…) işaret ediyordu. **Düzeltme:** sayfa pazarın kanıtlanmış `useListings({status:'active'})` hook'una bağlandı, ilanlar **kategori adına göre** (canlı `listing_categories.name`) filtreleniyor; `categoryMap` 10 gerçek slug/ad ile yeniden yazıldı (canlı `listing_categories` ile birebir); `condition` değerleri canlı enum'a hizalandı (`like-new`→`like_new`); yükleniyor durumu eklendi; `sitemap.ts` 10 gerçek slug'a çevrildi + var olmayan `/pazar/kategori` index'i çıkarıldı. `tsc` 0, build 0. **Temizlik tamamlandı (2026-06-07):** `mockListings` ölü sabiti **tamamen kaldırıldı** (dosyada ölü kod yok). **Kalan (izlenen, düşük öncelik):** SEO için ileride SSR'a taşınabilir (şu an client-fetch). ✅
- **Çözüldü (2026-06-07) — `isletmeler/[slug]/page.tsx` halka açık işletme profili tamamen sahteydi → gerçeğe bağlandı:** Sayfanın TÜM render'ı sabit `MOCK_BUSINESS` ("Moda Fırın": sahte 4.5 puan, 128 yorum, sahte çalışma saatleri, sahte hizmet listesi, sahte foto galerisi) + `MOCK_REVIEWS` (Ayşe K., Mehmet D. gibi uydurma kişilerin yorumları) + `MOCK_RECOMMENDATIONS` üzerine kuruluydu; gerçek `businesses` satırı yalnızca sahiplik/abonelik kontrolü için çekiliyordu. Sonuç: **geçersiz bir slug'da bile ziyaretçi gerçekmiş gibi görünen "Moda Fırın" + sahte yorumlar görüyordu** (mahallem-kart ile aynı sınıf güven/sorumluluk riski). Canlı veride 2 gerçek işletme var ("Moda Fırını ve Pastanesi", "Beşiktaş Kitabevi") ama ikisi de minimal: yalnızca ad/açıklama/adres/telefon dolu; `category_id`, `lat/lng`, `working_hours`, `logo_url`, `cover_url`, `website` = null, `is_verified=false`, `review_count=0`; `business_reviews` tablosu 0 satır. **Sayfa tamamen yeniden yazıldı:** gerçek `businesses` satırı slug ile çekiliyor; gerçek `business_reviews` (yazar adı için `profiles:user_id` join, ilişki yoksa boş-durum) çekiliyor; "diğer işletmeler" gerçek `businesses`'ten (varsa aynı kategori) geliyor. **Yükleniyor / "İşletme bulunamadı" / "Henüz yorum yapılmamış" / "başka işletme yok" durumları eklendi.** Veride OLMAYAN her bölüm sahte doldurulmak yerine gizleniyor: kategori, harita (lat/lng yok), çalışma saatleri (working_hours yok), hizmet listesi (DB kolonu yok → kaldırıldı), foto galerisi (kolon yok → kaldırıldı), e-posta (kolon yok → kaldırıldı), doğrulama rozeti (yalnızca `is_verified` true ise), puan/yıldız (yalnızca gerçek yorum varsa; yoksa "Henüz değerlendirilmemiş"). Uydurma "son güncelleme 9 Mart 2024" notu ve gerçek veri olmayan "Faydalı (helpful)" butonu silindi. Tüm `MOCK_*` sabitleri dosyadan **tamamen kaldırıldı** (artık ölü kod bile yok). `tsc` 0, test 141/141, build 0 (`/isletmeler/[slug]` ƒ Dynamic). ✅
- **Çözüldü (2026-06-07) — `pazar/ilan/[id]` (ilan detayı, `listing-detail-client.tsx`) sahte fallback + uydurma satıcı:** Sayfa zaten gerçek ilanı `getListingById` ile çekiyordu ama 3 ayrı sahtelik vardı: (1) **mock fallback** — gerçek ilan bulunamazsa veya fetch hata verirse `mockListingsDB`'den sahte "Laptop Lenovo / IKEA Kanepe / PS5" (id '1','2','3') gösteriyordu; (2) **gerçek satıcıya uydurma istatistik** — gerçek satıcı yolunda bile `rating: 4.8, reviewCount: 23, responseTime: '< 1 saat', joinDate: '2 yıl önce', listings: 45, soldCount: 42, verified: true` sabit enjekte ediliyor, kenar çubuğunda yıldızlarla gösteriliyordu (canlı `listings`/`profiles` şemasında bu alanlar YOK — join yalnızca `full_name, avatar_url, phone` döndürüyor); (3) **sabit-sahte "Benzer İlanlar"** — var olmayan id 4–7'ye linkli uydurma "Dell/HP/Asus/MacBook" kartları; ayrıca `favorite_count` kolonu canlı şemada YOK → "Kaydedilme" istatistiği hep 0/sahte. **Düzeltme:** `mockListingsDB` tamamen silindi → ilan yoksa zaten var olan "İlan Bulunamadı" durumu; satıcı kartı yalnızca gerçek alanları gösteriyor (ad, avatar — yoksa baş harf rozeti; uydurma puan/yorum/satış/tepki bloğu kaldırıldı); "Benzer İlanlar" gerçek `getListings({categoryId})` ile çekiliyor (kendisi hariç, yoksa bölüm gizli); görseli olmayan ilan için demo-stok yerine nötr "Görsel eklenmemiş" placeholder; redundant + sahte "İlan İstatistikleri" (Kaydedilme=0) bölümü kaldırıldı (görüntüleme zaten görsel üstünde gerçek `view_count`). Tüm `MOCK_*`/`getFeedImageUrl` sahteliği dosyadan çıktı. `tsc` 0, test 141/141, build 0 (`/pazar/ilan/[id]` ƒ Dynamic). ✅
- **Çözüldü (2026-06-07) — Ölü mock `notification-dropdown.tsx` silindi:** `src/components/layout/notification-dropdown.tsx` 10 sahte bildirim taşıyordu (içinde alarm veren uydurma **"Mahallenizde kayıp bir çocuk raporu verildi"** güvenlik uyarısı). Repo genelinde HİÇBİR yerden import edilmiyordu — navbar gerçek `NotificationBell`'i render ediyor, o da `useRealtimeNotifications()` ile gerçek; canlı bildirim UI'ı dürüst boş-durumlu ("Henüz bildirim yok"). Ölü kod olarak tamamen silindi (K2, git ile geri alınabilir). `tsc` 0, build 0. ✅
- **Çözüldü (2026-06-07) — `etkinlikler/[id]` (etkinlik detayı) %100 sahteydi + `etkinlikler` listesi mock fallback'li → ikisi de gerçeğe bağlandı:** Detay sayfası DB'ye HİÇ sormuyordu; tüm render sabit `mockEvent` ("Mahalle Kahvaltısı", uydurma organizatör "Ayşe Yılmaz", sahte katılımcılar, sahte yorumlar) + `similarEvents` (var olmayan id 2–4'e linkli) üzerineydi → geçersiz bir id'de bile ziyaretçi gerçekmiş gibi görünen dolu bir etkinlik + sahte yorumlar görüyordu. Liste sayfası `getEvents` çekiyordu ama DB boşsa 8 sahte etkinliğe (`mockEvents`) düşüyordu, organizatör görselleri hep demo-stoktu ve hafta/ay filtreleri sabit `'2026-03-10'` tarihine göre hesaplıyordu (bug). Canlı `events` şeması (execute_sql ile doğrulandı): id, organizer_id, title, description, location, lat/lng, start_date, end_date, cover_image, max_attendees, is_online, attendee_count, category. Dedike etkinlik-yorum tablosu YOK (`comments` yalnızca `post_id`'ye bağlı). **Düzeltme:** detay sayfası `getEventById` ile gerçek etkinlik + organizatör (`profiles`) + gerçek katılımcılar (`event_attendees`→`profiles`) çekiyor; yükleniyor / "Etkinlik bulunamadı" / "Henüz katılımcı yok" durumları eklendi; backing'i olmayan bölümler kaldırıldı (sahte yorumlar bölümü tamamen, sahte "Google Maps yakında" harita kutusu, uydurma organizatör bio + verified rozeti); RSVP gerçek `rsvpEvent` ile (kullanıcının kendi durumu gerçek kayıttan başlatılıyor); takvime-ekle/paylaş gerçek etkinlik verisinden; benzer etkinlikler gerçek `getEvents`'ten (aynı kategori, kendisi hariç, yoksa bölüm gizli); görseli olmayan etkinliğe demo-stok yerine nötr placeholder. Liste sayfası: `mockEvents` fallback'i + `getFeedImageUrl/getAvatarUrl` demo importları kaldırıldı → gerçek `events`; boşsa dürüst boş-durum ("Henüz etkinlik yok · ilkini sen oluştur" + Oluştur linki); yükleniyor durumu; organizatör görseli gerçek `avatar_url` (yoksa baş harf rozeti); hafta/ay filtreleri gerçek `new Date()`. **Latent bug fix:** `use-events.ts > rsvpEvent` var olmayan `event_attendees.id` kolonuna bakıyordu (`.select('id').single()` → her seferinde insert'e düşüp çakışabilirdi); bileşik `(event_id,user_id)` anahtarıyla `maybeSingle` + composite-eq update'e çevrildi. `tsc` 0, test 141/141, build 0 (`/etkinlikler` ○ Static, `/etkinlikler/[id]` ƒ Dynamic). ✅
- **Çözüldü (2026-06-07) — `isletmeler` liste + `isletmeler/kategori/[slug]` sayfaları %100 sahteydi → gerçeğe bağlandı + `getBusinesses` hook'u onarıldı:** Liste sayfası 10 sabit sahte işletme (`MOCK_BUSINESSES`: uydurma puan/mesafe/öne-çıkan/açık-kapalı, sahte id 1–10) gösteriyor ve DB'ye HİÇ sormuyordu; kartlar üstelik **sahte id ile** `/isletmeler/{id}`'ye linkliyordi (gerçek detay sayfası **slug** ile çekiyor → her kart kırık). Kategori sayfası ~1000 satırlık tamamen sahte `MOCK_BUSINESSES_BY_CATEGORY` idi; slug'ları (`restoran`, `kafe`, `kuafor`…) canlı `business_categories` slug'larıyla **uyuşmuyordu** ve `params.slug`'ı `use()` olmadan okuyordu (Next 16). Besleyen `getBusinesses` hook'u da bozuktu: var olmayan kolonlara bakıyordu (`.eq('verified',true)` → gerçek `is_verified`; `.order('rating')` → gerçek `rating_avg`; `.eq('neighborhood_id',…)` → businesses'ta böyle kolon YOK) ve hiç çağrılmıyordu. **Düzeltme:** `getBusinesses` gerçek şemaya yazıldı (`*, business_categories(name, slug)`, sıralama `rating_avg`→`created_at`, `categoryId` + `categorySlug` desteği `business_categories!inner` join ile, doğrulama filtresi yok → tüm kayıtlar). Her iki sayfa gerçek `businesses`'a bağlandı; kartlar **slug** ile detaya gider; uydurma mesafe/öne-çıkan/açık-kapalı ve sahte favori-toggle kaldırıldı; yükleniyor + dürüst boş-durumlar eklendi ("Henüz işletme eklenmemiş · ilkini sen ekle" / "Bu kategoride henüz işletme yok"); kategori çipleri yalnızca veride **gerçekten bulunan** kategorilerden türetiliyor; harita yalnızca lat/lng'si olan işletmeleri çiziyor (yoksa nazik not). `BusinessCard` dürüstleştirildi: `reviewCount===0` iken "0.0 (0)" + boş yıldız yerine **"Henüz değerlendirilmemiş"**; adres yoksa adres satırı gizli. `isletmeler/[slug]` detay sayfası `use(params)`'a geçirildi (Next 16 sağlamlaştırma). `sitemap.ts` işletme kategorileri 12 gerçek slug'a çevrildi + var olmayan `/isletmeler/kategori` index'i çıkarıldı (404). Eski sahte sabitler (`MOCK_BUSINESSES`, `MOCK_BUSINESSES_BY_CATEGORY`, `getFeedImageUrl` importları) dosyalardan **tamamen kaldırıldı** (ölü kod bile yok). Canlı veride 2 minimal işletme stub'ı var (kategori/puan/logo null) → sayfalar onları dürüstçe boş/"değerlendirilmemiş" gösteriyor. `tsc` 0, test 141/141, build 0 (`/isletmeler` ○ Static, `/isletmeler/kategori/[slug]` ƒ Dynamic). ✅
- **Çözüldü (2026-06-07) — `gruplar` liste + `gruplar/[slug]` + `gruplar/olustur` gerçeğe bağlandı:** Liste sayfası `useState(mockGroups)` ile **8 sahte grup** (uydurma ad/üye sayısı + `getGroupAvatarUrl`/`getGroupCoverUrl` demo görselleri gerçek gruplara) ile başlıyor, DB'ye yalnızca "veri varsa" bakıyor (boşsa sahteyi gösteriyor), kategori çipleri sabit (Ebeveynler/Spor/Hobi/Komşuluk — canlı `category` "Sosyal" ile uyuşmuyor) ve "Gruplarım" sayacı her zaman 0 (üyelik state'i boş başlıyordu). Detay sayfası **857 satırlık %100 sahteydi** (`mockGroupDetail`: uydurma "Kadıköy Spor Kulübü", sahte 128 üye/342 gönderi, sahte üye/gönderi/Medya/Etkinlik listeleri, sabit grup kuralları + sahte yöneticiler), DB'ye HİÇ sormuyor ve `params.slug`'ı `use()` olmadan okuyordu (Next 16). `olustur` formunda **sahte "Üye Davet Et"** (uydurma `mockMembers` kişileri, hiçbir yere yazılmıyordu), **"Grup Kuralları"** alanı (groups'ta kolon yok → sessizce yok sayılıyordu) ve **"Kapak Resmi"** yükleme (önizleniyor ama `createGroup` kaydetmiyordu) vardı. Canlı şema (execute_sql ile doğrulandı): `groups`(id,name,slug,description,cover_image,neighborhood_id,is_private,member_count,created_by,created_at,category — **avatar kolonu yok**), `group_members`(group_id,user_id,role,joined_at; user_id→profiles FK), `group_posts`(id,group_id,post_id — `posts`'a join tablosu), `posts`(…media_urls,title,body,author_id→profiles). Canlı veri: **1 grup, 0 üye, 0 gönderi**; tek grubun `neighborhood_id`'si null; **`group_posts`'ta yalnızca SELECT politikası var, INSERT YOK** → istemciden gönderi yazmak RLS ile imkânsız; grup için ayrılmış storage bucket'ı yok (mevcut: avatars/lending-images/listing-images/post-images). **Düzeltme:** hooks katmanına `getGroupBySlug`/`getGroupMembers`/`getGroupPosts`/`getUserGroupIds` eklendi (sayfalar ince kaldı, K1). Liste: `mockGroups` + demo importlar silindi → gerçek `getGroups`, `[]`+yükleniyor, dürüst boş-durum ("Henüz grup yok · ilkini sen oluştur"), kapak yoksa degrade + baş harf, kategori çipleri gerçek veriden, "Gruplarım" gerçek `group_members`'tan, Katıl butonu yalnızca girişli kullanıcıya. Detay: `use(params)` + gerçek grup/üyeler(`group_members`→profiles)/gönderiler(`group_posts`→posts→profiles); sekmeler **Gönderiler/Üyeler/Hakkında** (Medya + Etkinlik sekmeleri backing tablo olmadığı için kaldırıldı; sahte kurallar + sahte yöneticiler kaldırıldı); **sahte gönderi kompozisyon kutusu kaldırıldı** (group_posts INSERT politikası yok → salt-okunur, dürüst boş-durum); üye/gönderi sayıları canlı `length`'ten; avatar yoksa baş harf; Katıl/Ayrıl gerçek `joinGroup`/`leaveGroup` ile; kurucuya "kurucususunuz" rozeti; yükleniyor / "Grup Bulunamadı" durumları. `olustur`: sahte üye-davet + kaydedilmeyen kurallar + kaydedilmeyen kapak yükleme kaldırıldı → form yalnızca gerçekten kaydedilen alanları (ad/açıklama/kategori/gizlilik) topluyor, başarıyla oluşunca yeni grubun slug'ına yönlendiriyor. Tüm sahte sabitler dosyalardan tamamen çıktı (ölü kod bile yok). `tsc` 0, test 141/141, build 0 (`/gruplar` ○ Static, `/gruplar/[slug]` ƒ Dynamic, `/gruplar/olustur` ○ Static). **Ertelenen gerçek altyapı (ileride):** (1) `groups.rules` kolonu + create/detay'da gösterim; (2) `group-images` bucket + RLS → gerçek kapak yükleme; (3) `group_posts` INSERT politikası + grup gönderi oluşturma akışı (posts insert için neighborhood_id çözümü gerekir). ✅
- **Çözüldü (2026-06-07) — Navbar hızlı-arama (`search-dropdown.tsx`) 5 sahte sonuç → gerçek canlı arama:** Navbar'daki açılır arama kutusu `mockResults` ile **5 uydurma sonuç** ("Komşu toplantısı", sahte "342 üye"li grup vb.) gösteriyor ve **var olmayan rotalara** (`/posts/1`, `/listings/2`…) linkliyordu. Tamamen yeniden yazıldı: her tuş vuruşunda **300 ms debounce** ile canlı Supabase'te `ilike` araması (posts/businesses/profiles/events/groups/listings, her biri `.limit(4)`), doğru rotalar (`/gonderi/[id]`, `/isletmeler/[slug]`, `/profil/[id]`, `/etkinlikler/[id]`, `/gruplar/[slug]`, `/pazar/ilan/[id]`), listings `.eq('status','active')` filtreli, yükleniyor (Loader2) / "Sonuç bulunamadı" / "Aramak için bir kelime yazın" dürüst durumları, footer "Tüm sonuçları gör" → `/ara?q=`. `ara/page.tsx`'teki kanıtlanmış kalıbın aynısı. `tsc` 0. ✅
- **Çözüldü (2026-06-07) — Son sahte admin yüzeyi `admin/odemeler` gerçek `payments` tablosuna bağlandı + admin RLS eklendi:** Sayfa `MOCK_PAYMENTS` (20 uydurma kayıt) + uydurma istatistik (`monthlyRevenue = weeklyRevenue * 2.5` gibi) gösteriyordu. Baştan yazıldı: `useQuery` ile gerçek `payments` (created_at desc, limit 1000) çekiliyor, `user_id`'ler `profiles`'tan isimle eşleştiriliyor (yoksa "Bilinmiyor"); gerçek gelir matematiği **kayan pencerelerle** (son 24s / 7g / 30g, yalnızca `status==='completed'`) — **çarpan yok**; TYPE/STATUS/METHOD etiketleri `payment.ts` enum'larıyla birebir; arama/tür/durum filtreleri + sayfalama (10/sayfa) + detay modalı; yükleniyor (Loader2) / hata ("Ödemeler yüklenemedi") / boş ("Henüz ödeme kaydı yok") durumları. **DB:** sahip-bazlı RLS admin okumasını engelliyordu → projenin yerleşik admin kalıbıyla (`EXISTS … profiles.is_admin`) additive `pay_select_admin` SELECT politikası eklendi (sahip politikası korundu; SELECT politikaları OR'lanır). İlişkili `MockDataBanner` bileşeni + `admin/layout.tsx` bağlaması silindi. `tsc` 0, build 0, test 141/141. ✅
- **Çözüldü (2026-06-07) — `post-form-modal.tsx` DB hatasında SAHTE "başarılı gönderi" üretiyordu → dürüst hata:** Gönderi oluşturma başarısız olduğunda (`createPost` error) `else` dalı `fallbackPost = { id: Date.now().toString(), … }` uydurup `onSubmit(fallbackPost)` ile feed'e **kaydedilmemiş gönderiyi "yayınlanmış" gibi** ekliyordu (kullanıcı paylaştım sanıyor, DB'de yok — pazar için gerçek veri-kaybı/güven bug'ı). Sahte-başarı kaldırıldı → artık `toast.error('Gönderi paylaşılamadı…')`; eksik `catch` eklendi (beklenmeyen hata da dürüstçe bildiriliyor); `finally`'de `setIsSubmitting(false)`. Başarı dalı (gerçek `newPost`) değişmedi. `tsc` 0. ✅
- **Çözüldü (2026-06-07) — `blog/[slug]` sahte yorumları + ölü CTA dürüstleştirildi:** Blog yazı detayında **uydurma iki yorum** ("Merve Sarı", "Oğuzhan Kılıç" — sahte sosyal kanıt; App Store "deceptive content" riski) + **çalışmayan yorum formu** (`onClick` yok → hiçbir yere yazmaz) vardı; ayrıca "Hemen Başla" CTA'sı ölü butondu (`onClick` yok). Sahte yorumlar + ölü form **dürüst "Yorumlar çok yakında" boş-durumuyla** değiştirildi; "Hemen Başla" gerçek `/kayit` rotasına bağlandı (`<Link>`). `next build` 0, test 171/171. ✅
- **Kalan — SAHİP KARARI (içerik, çevrimdışıyken yapılmadı):** `blog/page.tsx` (liste) hâlâ 12 yazıyı **ölü `#post-N` çapalarına** linkliyor (gerçek `/blog/[slug]` rotası varken) ve listelenen 12 başlıktan yalnızca **3'ünün gerçek gövdesi** var (`blog/[slug]` içindeki `blogArticles`). Geri kalan 9 başlık tıklanınca "Yazı Bulunamadı"ya düşer. Bu bir **içerik/pazarlama kararı** (gerçek yazılar yazılacak mı, yoksa liste 3'e mi indirilecek?) — sahip çevrimdışıyken tek taraflı kırpmadım. Ayrıca liste sayfasındaki bülten "Abone Ol" butonu da bağlanmamış (backend ister). **Hedef: sahip içerik kararı + sonra liste→slug bağlama.**
- **Ödeme planı (kalan):** Geriye kalan son-kullanıcı mock fallback'leri gerçek boş-durum (empty state) bileşenleriyle değiştir. Çoğu düşük-risk ama tek tek runtime testi ister. **Hedef: Faz 1–2 (kademeli).**

### 13. İçerik şikâyet (report) akışı sahteydi → gerçeğe bağlandı + UI'a takıldı (2026-06-07)
- **Ne / nerede:** `src/components/shared/report-modal.tsx` kullanıcının seçtiği nedeni alıyor ama **hiçbir yere kaydetmiyordu** (`// Simulate API call` + `setTimeout(1500)` → her zaman sahte "başarılı"; `type`/`targetId` prop'ları kullanılmadan atılıyordu). Üstelik modal **hiçbir yerden açılamıyordu** — `feed/post-card.tsx`'teki üç-nokta (`MoreHorizontal`) butonunun `onClick`'i yoktu (ölü buton).
- **Etki:** Yüksek (K10 mağaza uyumu). Apple Guideline 1.2 + Google Play UGC politikası **çalışan** bir içerik-şikâyet mekanizması ZORUNLU kılar; sahte "şikâyet alındı" hem mağaza reddi hem kullanıcıyı yanıltma.
- **Keşif:** Backend ZATEN tam kuruluydu — `reports` tablosu + RLS (insert: `auth.uid() = reporter_id`) + `admin/moderasyon` kuyruğu hem `reports` hem `content_moderation`'ı okuyup şikâyetleri turuncu "Şikâyet" etiketiyle gösteriyor. Sadece kullanıcı-yüzü submit'i sahteydi.
- **Yapıldı:** (1) ReportModal gerçek `insert` yapıyor: `post`/`comment`/`user` → `reports` (`reporter_id=user.id`, ilgili `post_id`/`comment_id`/`reported_user_id`, neden + opsiyonel açıklama); `listing` → `content_moderation` (`content_type='listing'`, `reported_by=user.id`, `status='pending_admin'`). Giriş yoksa "giriş yapın" uyarısı, hata olursa dürüst hata mesajı (sahte başarı YOK). Her iki tablonun şeması + RLS'i `execute_sql` ile canlıda doğrulandı. (2) `post-card.tsx`'teki ölü üç-nokta butonu küçük bir menüye (`Şikâyet Et`) bağlandı → modalı `type="post" targetId={post.id}` ile açıyor (dışarı tıkla-kapat görünmez katman + erişilebilir `role="menu"`).
- **Not — `tsc` vs `next build` tuzağı:** `tsconfig.json`'da `incremental:true` olduğu için standalone `npx tsc --noEmit` bayat `.tsbuildinfo` ile **yanlış yeşil** verdi; `next build` (taze tip denetimi) Supabase `.insert()`'in `Record<string,string>` reddini yakaladı. **Ders: tip kapısı için yetkili gate `next build`.** Düzeltme: insert'e jenerik Record yerine somut nesne-literali (eksik kolonlara açık `null`) verildi. `next build` 0, test 150/150, lint 0. ✅
- **Tetikleyici yayılımı (2026-06-07, devam):** Şikâyet tetikleyicisi artık üç gerçek yüzeyde:
  - **post** ✅ — `feed/post-card.tsx` üç-nokta menüsü (önceki tur).
  - **ilan** ✅ — `pazar/ilan/[id]/listing-detail-client.tsx`'teki ölü "Bu ilanı bildir" butonu `ReportModal type="listing" targetId={listing.id}`'ye bağlandı.
  - **profil** ✅ — `profil/[id]/page.tsx`'te başkasının profilinde (`!isOwnProfile`) "Şikâyet Et" butonu eklendi → `ReportModal type="user" targetId={targetUserId}`.
  - Doğrulama: `next build` 0, test 150/150; lint yalnızca önceden var olan `any` borcu (yeni hata yok).
- **Kalan — yorum şikâyeti (`type="comment"`):** Bilinçli ATLANDI. Sebep: uygulamada şu an **bireysel yorumu id'siyle gösteren bir yüzey yok** — `post-card.tsx` yalnızca yorum *yazma* kutusu + sayı gösteriyor; yorumları id'leriyle listeleyen `feed/comment-section.tsx` ise **hiçbir sayfada mount edilmiyor** (ölü bileşen, yalnızca barrel'da export'lu). Ölü bileşene buton koymak busywork olur (SADELİK). Modal `type="comment"`'i zaten destekliyor; yorum listesi gerçekten render edildiğinde aynı `MoreVertical` butonuna bağlanır. **Hedef: yorum listesi UI'ı geldiğinde.**

### 14. Kimlik/adres doğrulama eşleştirme mantığı route'tan çıkarıldı + test edildi (K1/K3/K4, 2026-06-07)
- **Ne / neden:** `src/app/api/verify-document/route.ts`, kullanıcının yüklediği adres belgesini turkiye.gov.tr'den kazınan kayıtla karşılaştırıp kişiyi **"doğrulanmış mahalle sakini"** yapar — bu, mahalle-güven modelinin GÜVENLİK KAPISIDIR. Saf karar mantığı (eşleştirme/parse) route'un içinde, puppeteer I/O ile iç içe ve **test edilemez** haldeydi. Eşik gevşerse yanlış kişi mahalleye girer, sıkılaşırsa gerçek sakin reddedilir.
- **Yapıldı:** Saf 4 fonksiyon (`normalizeText`, `compareAddresses`, `compareDocuments`, `parseEdevletResult`) davranış birebir korunarak **sıfır-I/O modüle** taşındı → `src/lib/services/edevlet-match.ts` (K1: ince route + test edilebilir domain; `paytr-oid.ts` deseninin aynısı). Route artık bunları import ediyor (`scrapeEdevlet`→`parseEdevletResult`, `POST`→`compareDocuments`); yalnızca `POST` export'lu kaldı (Next route-export doğrulaması korunur). 15 yeni test (`src/__tests__/edevlet-match.test.ts`) güvenlik sözleşmesini kilitliyor: Türkçe harf folding (İ→I…), %60 adres-örtüşme eşiği (üstü geçer/altı geçmez), ve **bilinçli GEVŞEK kararlar** — `scraped.error`→ret, karşılaştırılacak alan yoksa→doğrula (barkod+TC zaten sorgulandı), ad uyuşmasa bile adres eşleşirse→doğrula (`matchCount>0`), kısmi il/ilçe/mahalle dalı; ayrıca parse'ın ad/adres/tarih çıkarımı + "bulunamadı"→error. Gelecekte biri eşiği sessizce değiştirirse test kırılır.
- **Doğrulama:** `npm run build` 0 (route compile + export doğrulaması), taze `tsc --noEmit --incremental false` 0, test **194/194**.
- **Not (kapsam):** `scrapeEdevlet` (puppeteer/headless Chrome I/O) ve route'un rate-limit + format validasyonu route'ta kaldı (I/O bağımlı, ünite testi puppeteer mock'u gerektirir — düşük getiri). Otomatik scrape başarısızsa belge zaten manuel admin onay kuyruğuna (`address_verifications` pending) düşüyor — eşleştirme mantığı hatası bu insan-yedeğiyle de yakalanır.

### 15. Yasal metinler güncel Türk mevzuatına göre yeniden yazıldı + İşletim Kılavuzu oluşturuldu (K5/K9, 2026-06-07)
- **Ne / neden:** Üç yasal sayfa (`/kvkk`, `/gizlilik`, `/kosullar`) mevcut haliyle hem **hukuken yanlış** hem de uygulamanın **gerçek veri akışıyla tutarsızdı** → mağaza reddi (K10) + KVKK uyum riski. Önce canlı Supabase şeması (`profiles`/`address_verifications`/`payments`) + `api/verify-document/route.ts` `execute_sql`/okuma ile doğrulandı: **TC Kimlik No SAKLANMIYOR** (e-Devlet doğrulaması anlık), **kart verisi SAKLANMIYOR** (PayTR işliyor) — bu gerçekler metinlere uyum lehine işlendi.
- **Yapıldı — `kvkk/page.tsx` (KVKK Aydınlatma Metni, 6698 sayılı Kanun Md. 10 uyumlu, sunucu bileşeni):** Haklar artık doğru **Madde 11**'e dayanıyor (eski metin yanlışlıkla "Madde 12 = veri güvenliği"yi gösteriyordu). Eklenen zorunlu açıklamalar: veri sorumlusu kimliği, işleme amaçları (8), **hukuki sebep (Md. 5/2-c/ç/e/f + açık rıza)**, **toplama yöntemi**, aktarım (Md. 8) + **yurt dışı aktarım (Md. 9, 7499/2024 sonrası: yeterlilik kararı/uygun güvence/açık rıza)**, saklama süresi (hesap→30 gün, ödeme→10 yıl mali mevzuat), başvuru usulü (Tebliğ, 30 gün), Kurul şikâyet hakkı (kvkk.gov.tr). Günlük değişen `new Date()` yerine sabit `EFFECTIVE_DATE`. TC kimlik/kart SAKLANMAZ açıkça yazıldı.
- **Yapıldı — `gizlilik/page.tsx` (Gizlilik Politikası):** KVKK metniyle hizalandı; bozuk "taleplerini yoksayarak" cümlesi düzeltildi; TC kimlik+kart saklanmıyor yeşil notu, yurt dışı aktarım alt-bölümü, "Çocukların Gizliliği (18+)" bölümü, güvenlikte RLS açıklaması eklendi; Türkçe-karakter id'leri normalize edildi; daktilo hataları (özelikleri/engelliyebilir…) düzeltildi.
- **Yapıldı — `kosullar/page.tsx` (Kullanım Koşulları):** **6502 sayılı Tüketicinin Korunması Hakkında Kanun + Mesafeli Sözleşmeler Yönetmeliği** uyarınca yeni "**Ücretli Hizmetler ve Cayma Hakkı**" bölümü (14 gün cayma + dijital/anında ifa istisnası, PayTR, iade) ve "**Uygulanacak Hukuk ve Yetkili Mahkeme**" bölümü (Türk hukuku, İstanbul mahkemeleri, Tüketici Hakem Heyetleri) eklendi; argo/yabancı terimler ("intimidasyon"→"yıldırma", "scam"→"dolandırıcılık") düzeltildi.
- **Yapıldı — `RUNBOOK.md` (İşletim Kılavuzu, K9) oluşturuldu:** 12 bölüm — sistem haritası, env/sırlar, deploy prosedürü (coskun→main→Vercel + CI kapıları), Supabase yedek/geri-yükleme/migration, hata izleme (K8), olay müdahale + rollback, anahtar rotasyonu (PayTR/Supabase service_role), **KVKK veri-sahibi başvuru prosedürü (30 gün)**, hesap silme & saklama operasyonu, felaket kurtarma, bakım takvimi, yayın öncesi son kontrol listesi. Gerçek dosya/komut/env adlarıyla grounded.
- **Doğrulama:** taze `tsc --noEmit --incremental false` 0, test **194/194**, `next build` 0 (`/kvkk` + `/kosullar` + `/gizlilik` ○ Static derlendi).
- **Kalan — SAHİP/AVUKAT İŞİ (otomatik yapılamaz, yayın öncesi ZORUNLU):** (1) Şirket sicil bilgileri yasal metinlere işlenecek: tam ticari unvan, **açık adres, MERSİS no, VERBİS kayıt no, KEP adresi** (şu an `/kvkk`'de "yayın öncesi tamamlanacak" sarı notuyla bekliyor). (2) Üç metnin **avukat onayı**. (3) VERBİS kaydı (eşik aşılıyorsa). Bu maddeler `RUNBOOK.md` Bölüm 12 son-kontrol listesinde ★ ile işaretli. **Hedef: yayın öncesi (sahip + avukat).**
