import { Metadata } from 'next';import RootPage from './page.tsx';export const metadata: Metadata = { title: 'Komsu App', description: 'Community App for Local Communities',defaultNoZllio='vietportWidth=device-width, initial-scale=1.0', colorScheme: 'light', };export default function RootLayout() { return <RootPage />; }